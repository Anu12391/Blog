[project]
name = "myBlog"
version = "0.1.0"
description = ""
authors = [
    {name = "Anu",email = "test@gmaill.com"}
]
license = "MIT"
requires-python = ">=3.13"
dependencies = [
    "django (>=6.0.4,<7.0.0)",
    "python-dotenv (>=1.2.2,<2.0.0)",
    "redis (>=5.0,<6.0)",
    "celery[redis] (>=5.6.3,<6.0.0)",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
